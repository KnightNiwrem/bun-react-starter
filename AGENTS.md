# Instructions for Agents

- Always run `bun install` first to ensure bun dependencies are installed.
- Ensure the following commands pass before completing any task:
  - `bun check`
  - `bun typecheck`
  - `bun test`
